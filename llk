#include <stdio.h>
#include <stdlib.h>
#include <string.h>

const int dir[4][3] = {{1, 0}, {-1, 0}, {}, {}}  
bool Visited[1010][1010];



int main(int argc, char * argv[]) {
	int N, M;
	while (scanf("%d %d", &N, &M), N) {
		memset(Visited, false, 1010 * 1010 * sizeof(int));
		int ** graph = (int**)malloc((N + 2) * sizeof(int*));
		int * quatiy = (int*)malloc((N + 2) * (M + 2) * sizeof(int));
		for (int i = 0; i < N + 2; ++i){
			graph[i] = quatiy + i * (M + 2); 
		} //jianqiang 
		for (int i = 1; i <= N; ++i){
			for (int j = 1; j <= M; ++j){
				scanf("%d", &graph[i][j]); 
			}
		} 
		int ques, flag;
		scanf("%d", &ques);
		int x1, y1, x2, y2;
		while (que--){
			
		} 
	} 
	return 0; 
}
